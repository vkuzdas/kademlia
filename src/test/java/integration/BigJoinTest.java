package integration;

import kademlia.KademliaNode;
import org.junit.jupiter.api.Test;

import java.io.IOException;
import java.math.BigInteger;
import java.time.Duration;

public class BigJoinTest extends IntegrationBaseTest {

//    @Test
//    public void testJoin50() throws IOException, InterruptedException {
//        KademliaNode.setRepublishInterval(Duration.ofSeconds(13));
////        KademliaNode.setExpireInterval(Duration.ofSeconds(30));
//        KademliaNode.setRefreshInterval(Duration.ofSeconds(10));
//
//        for (int i = 0; i < 2; i++) {
//            KademliaNode joiner = new KademliaNode(LOCAL_IP, BASE_PORT++);
//            if (runningNodes.isEmpty())
//                joiner.initKademlia();
//            else
//                joiner.join(getRandomRunningNode().getNodeReference());
//            runningNodes.add(joiner);
//        }
//        Thread.sleep(1000*60);
//    }
}
